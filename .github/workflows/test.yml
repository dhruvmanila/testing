on:
  push:
    branches: master
  pull_request:
    branches: master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
         echo $GITHUB_RUN_ID
         echo $GITHUB_ACTION
         echo $GITHUB_EVENT_PATH
         echo $GITHUB_SHA
         echo $GITHUB_REF
         echo $GITHUB_API_URL
      - shell: bash
      
