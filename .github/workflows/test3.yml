on: [push, pull_request]
jobs:
  timepass:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
        echo $GITHUB_SHA
        echo $GITHUB_REF
        echo $GITHUB_EVENT_PATH
